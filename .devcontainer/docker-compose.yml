version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    environment:
      - NODE_ENV=development
    ports:
      - "4200:4200"
    networks:
      - clinical-net

networks:
  clinical-net:
    driver: bridge
